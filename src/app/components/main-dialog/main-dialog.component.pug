.modal-header
  h4.modal-title {{ article.name }}
  button.close(type='button', aria-label='Close', (click)="closeModal()")
    span(aria-hidden='true') ×
.modal-body
  img([src]='article.image.url')